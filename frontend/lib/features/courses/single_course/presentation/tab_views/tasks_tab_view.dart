import 'package:flutter/material.dart';

class TasksTabView extends StatelessWidget {
  const TasksTabView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.task, size: 350);
  }
}
