import 'package:flutter/material.dart';

class RatingTabView extends StatelessWidget {
  const RatingTabView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.star, size: 350);
  }
}
